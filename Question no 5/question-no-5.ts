// Choose your favorite person and quote
const famousPerson: string = "Stephen Hawking";
const quote: string = "Intelligence is the ability to adapt to change.";

// Compose the message using string interpolation
const message: string = `${famousPerson} once said, "${quote}"`;

// Print the message
console.log(message);
